# Supabase (https://supabase.com) — copy to .env.local for local dev
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Apply SQL in supabase/migrations/001_auth_core.sql via Supabase SQL editor (or CLI).
# In Authentication → Providers, disable "Confirm email" for faster local testing, or expect email confirmation after signUp.
